package cloud.autotests.tests;

import cloud.autotests.helpers.DriverUtils;
import io.qameta.allure.Description;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static com.codeborne.selenide.Selenide.open;
import static com.codeborne.selenide.Selenide.title;
import static io.qameta.allure.Allure.step;
import static org.assertj.core.api.Assertions.assertThat;


public class GeneratedTests extends TestBase {
    @Test
    @Description("Soon to be implemented by you (or QA.GURU engineers)")
    @DisplayName("Autotest_RSHB")
    void generatedTest() {
        step("- Зайти на сайт rshb.ru", () -> {
            step("// todo: just add selenium action");
        });

        step("- Кликнуть «Частным лицам»", () -> {
            step("// todo: just add selenium action");
        });

        step("- Кликнуть «Кредиты»", () -> {
            step("// todo: just add selenium action");
        });

        step("- Кликнуть «Потребительский кредит без обеспечения»", () -> {
            step("// todo: just add selenium action");
        });

        step("- Расчитать кредит", () -> {
            step("// todo: just add selenium action");
        });

        step("- тип платежа «Аннуитентный»", () -> {
            step("// todo: just add selenium action");
        });

        step("- сумма кредита 2 500 000 р", () -> {
            step("// todo: just add selenium action");
        });

        step("- Срок кредита 60 м", () -> {
            step("// todo: just add selenium action");
        });

        step("- кликнуть чекбокс «Получаю зарплату на счёт в россельхозбанке», остальные чекбоксы не должны быть активны", () -> {
            step("// todo: just add selenium action");
        });

        step("- Сравнить что ставка = 12.4%, а ежемесячный платеж = 56118", () -> {
            step("// todo: just add selenium action");
        });
    }
}